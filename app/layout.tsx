import type { Metadata } from 'next';import './globals.css';const title='学分导航｜2024级培养方案助手';const description='粘贴成绩，自动核验培养方案要求与毕业进度。';const basePath=process.env.NEXT_PUBLIC_BASE_PATH||'';export const metadata:Metadata={metadataBase:new URL(process.env.NEXT_PUBLIC_SITE_URL||'https://yanzi1-creator11.github.io'),title,description,openGraph:{title,description,images:[{url:`${basePath}/og.png`,width:1200,height:630}]},twitter:{card:'summary_large_image',title,description,images:[`${basePath}/og.png`]}};export default function RootLayout({children}:{children:React.ReactNode}){return <html lang="zh-CN"><body>{children}</body></html>}
